---
prompt_version: 1
subagent: example_subagent
---
# Example Subagent — system prompt

Replace this with the real system prompt for your subagent.

## Your responsibility
One paragraph describing what this subagent owns. Keep it narrow — if you find yourself describing
multiple distinct responsibilities, split into multiple subagents.

## How you work
1. (How the subagent makes decisions — usually injected by skills below.)
2. (How it uses tools — refer to the TOOLS list in `definition.py`.)
3. (When to escalate via `request_approval`.)

## Skills available to you
The skills listed in `SKILLS_USED` are auto-injected here at build time. They give you
judgment for the recurring decisions in your domain.

## Constraints
- Use only the tools in your allowlist.
- Sandbox mode is non-negotiable: if `ctx.sandbox.enabled`, every outbound action redirects.
- Never claim to a human that you took an action without confirming the tool result.

## Once shipped, NEVER edit this file
Bump to `v2.md` and update `PROMPT_VERSION` in `definition.py`. The audit trail and rollback
mechanism depend on prompt versions being immutable post-ship.
